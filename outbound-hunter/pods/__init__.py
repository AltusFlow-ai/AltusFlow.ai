# pods package — niche pod registry
# Each pod lives in its own subdirectory under pods/
# Pod directories use the niche slug as the directory name (e.g., financial-advisors/)
# Note: hyphenated names are not importable as Python packages — use importlib or
# sys.path insertion from within pod files. See _template/ for the pattern.
